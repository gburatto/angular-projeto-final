import { VegetarianoPipe } from './vegetariano.pipe';

describe('VegetarianoPipe', () => {
  it('create an instance', () => {
    const pipe = new VegetarianoPipe();
    expect(pipe).toBeTruthy();
  });
});
