import { FiltrarPorTipoPipe } from './filtrar-por-tipo.pipe';

describe('FiltrarPorTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPorTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
